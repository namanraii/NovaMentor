# Frontend environment — copy to frontend/.env.local
# This tells React where the FastAPI backend is running

# Local development
VITE_API_URL=http://localhost:8000

# Production (replace with your Render backend URL after deployment)
# VITE_API_URL=https://careerforge-ai.onrender.com
